services:
  discord-qnap-bot:
    build: .
    container_name: TestBot
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
      - TZ=Europe/Berlin
    env_file:
      - .env
    volumes:
      - ./data:/app/data
